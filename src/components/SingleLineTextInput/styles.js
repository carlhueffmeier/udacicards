import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    padding: 15,
    borderWidth: 1,
    borderRadius: 10,
    margin: 30,
    alignSelf: `stretch`
  },
  input: {
    fontSize: 20
  }
});
